## 오늘 한 일(200820)

    - 프로젝트
        - build 과정에서 src 폴더에 view와 static을 build 파일에 복사에서 올려야하는데 윈도우에서는 cp 커맨드가 안먹힘.. (계속 안되는중..)

        - => 해결 : xcopy로 xcopy [src] [dest] 로 해결 했다!

        - package.json에서 경로는 백슬래쉬 두번으로 경로를 끊는다!!

#### 내일 할 일(200821)

    -프로젝트
      build 과정 올리기
